<h3>Bob Flether's</h3>
<h1>BURGER'S DIGEST</h1>

<div class="container">
  <div class="row">

    <h4>Get a burger from Bob...</h4>

    <form class="random-form">
      <button id="random-submit" type="submit">Random Bob's Burger</button>
    </form>

    <h4>... or come up with your own yummy punny burger!</h4>

    <form class="create-form">
      <div class="form-group">
        <input type="text" id="burg" name="name" placeholder="Add your own burger...">
      </div>
      <button id="create-submit" type="submit">Add My Burger</button>
    </form>

  </div>

  <div class="row">
    <div class="col-sm">

      <h2>Burgers Served</h2>

      <ul>
        {{#each burgers}}
        {{#unless digested}}
        {{> burgers/burger-block digest=true}}
        {{/unless}}
        {{/each}}
      </ul>

    </div>

    <div class="col-sm">

      <h2>Burgers Digested</h2>

      <ul>
        {{#each burgers}}
        {{#if digested}}
        {{> burgers/burger-block digest=false}}
        {{/if}}
        {{/each}}
      </ul>

    </div>
  </div>
</div>